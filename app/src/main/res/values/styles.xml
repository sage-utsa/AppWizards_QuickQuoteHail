<resources>
    <!-- Spinner dropdown fix for dark theme -->
    <style name="SpinnerPopupDark" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">@color/colorSurface</item>
    </style>
</resources>
